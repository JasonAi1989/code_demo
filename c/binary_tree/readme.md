这个不会主要是编写了二叉树的使用，包括插入、删除和遍历。

遍历分为前序遍历、中序遍历和后序遍历三种。前中后是按照访问一个单元（包括父节点，左子树，右子树三个部分）时访问父节点的时间节点划分的。

前序遍历：DLR，访问一个单元中三个部分的顺序为

    父节点->左子树->右子树
    
中序遍历：LDR，访问一个单元中三个部分的顺序为

    左子树->父节点->右子树
    
后序遍历：LRD，访问一个单元中三个部分的顺序为

    左子树->右子树->父节点
    
最常用的遍历方式为递归